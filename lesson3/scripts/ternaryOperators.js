// ternary operators

const age = 18;
const drink = age >= 18 ? 'wine' : 'water';
// console.log(drink);
// ternary operators are expressions, not statements

// nested ternary operators

const lessonSubject = 'science';
const excercise = lessonSubject === 'maths' ? 'algebra' : lessonSubject === 'science' ? 'physics' : 'english';

console.log(excercise);